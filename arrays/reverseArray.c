#include <stdio.h>

void reverse(int arr[]);
void swap();

int main()
{

    int arr[] = {1, 2, 3, 4, 5, 6, 7, 8, 9};

    // we are going to reverse an array

    reverse(arr);

    return 0;
}

void reverse(int arr[])
{
    //~we are going to reverse an array using two pointers method

    int start = 0;
    // int end = arr.
}